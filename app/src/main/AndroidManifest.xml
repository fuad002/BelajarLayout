<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.belajarlayout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/fuad"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/fuad"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity_drop"></activity>
        <activity android:name=".TesConstrainLayout"></activity>
        <activity android:name=".TesRelativeLayout" />
        <activity android:name=".TesLinearLayout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>